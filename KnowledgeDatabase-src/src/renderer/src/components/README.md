# 公共组件 (Components)

全局通用的 UI 组件，每个组件一个文件夹。

## 规范

- 组件目录使用 PascalCase 命名
- 每个组件目录包含 `index.vue` 作为入口
- 组件内部类型定义放在组件目录内

## 示例结构

```
components/
├── BaseButton/
│   └── index.vue
├── BaseInput/
│   └── index.vue
└── ...
```
